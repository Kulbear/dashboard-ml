export const appStyles = () => ({
  appContainer: {
    height: '100%',
    backgroundColor: 'black',
    // TODO: the background image does not work
    // background: 'url(../img/site-bg-v2.jpg)'
  }
});

export const dashboardStyles = () => ({
  root: {
    fontSize: 100,
    color: 'white'
  }
});